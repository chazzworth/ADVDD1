{
  "name": "advdd1-root",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install-all && npm run build --prefix client && npx prisma generate --schema=./server/prisma/schema.prisma",
    "start": "node server/index.js"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
